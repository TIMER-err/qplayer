# Reference clones — source-mined, not part of this repo
/qml4j/
/Haedus/

# Build output
target/
build/
.gradle/
*.class

# Local machine config (Android SDK path, Termux toolchain overrides, ...)
local.properties

# IDE
.idea/
*.iml
.vscode/

# App runtime data
.qplayer/

# log4j2 rolling output (created at runtime next to the app / module dir)
logs/

# Generated classpath file (mvn dependency:build-classpath), regenerated locally
desktop-host/cp.txt

# One-off local scripts, kept on disk but never committed
desktop-host/src/main/java/dev/t1m3/qplayer/desktop/tools/

# Local dev memo — never committed
/本地开发备忘.md
.tmp/

# JVM crash dumps. This machine runs close to its memory limit, so a build or
# test JVM occasionally dies and drops these in the repo root.
hs_err_pid*.log
replay_pid*.log
